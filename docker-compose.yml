version: '3.1'
services:


  web:
    build: .
    command: python -u app.py
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    network_mode: bridge
    depends_on:
      - db
    links:
      - db

  db:
    image: mongo:latest
    network_mode: bridge
